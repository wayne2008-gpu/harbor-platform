# Synthetic Trajectory Sample Smoke Task

Create `/workdir/synthetic-smoke-output.txt` with the exact text
`synthetic-trajectory-sample-ok`.

Also write a valid ATIF trajectory to `/logs/agent/trajectory.json` and a sample
payload to `/logs/artifacts/samples.json`.
